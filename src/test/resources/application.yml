spring:
  profiles:
    active: test
    #active: performance
    include:
      - aws
      - oauth
      - smtp
  messages:
    basename: messages

  #Spring boot 2.6 이상 & swagger 3.0 사용시 호완 오류
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

logging:
  level:
    root: info